"""SDR bounded context.

This package owns lead intake orchestration, qualification, routing, and CRM
handoff contracts. Provider-specific code belongs in ``integrations`` and
generic execution concerns belong in ``automation``.
"""
